import Vue from "vue";

import Vuex from "vuex";

Vue.use(Vuex);

//基础信息
const baseInfo = {
  nowDate: object
};
const searchInfo = {
  isMonth: boolean
};

const state = {
  baseInfo,
  searchInfo
};
